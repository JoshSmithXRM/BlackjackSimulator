namespace Blackjack
{
    public class Program
    {
        public static void Main(string[] args)
        {
            int deckCount = 8; // Specify the number of decks
            CompositionRoot.Compose(deckCount);
        }
    }
}

